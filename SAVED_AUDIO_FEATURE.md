# 🎵 Функция сохранения аудио файлов

## Что добавлено

### Backend
1. **API endpoint** `/api/saved-audio`:
   - `GET /` - получить все сохраненные аудио
   - `POST /` - сохранить аудио файл
   - `DELETE /:messageId` - удалить сохраненное аудио

2. **База данных**:
   - Таблица `saved_audio` для хранения ссылок на сохраненные аудио
   - Индексы для быстрого поиска

### Frontend
1. **Профиль пользователя**:
   - Вкладка "Аудио" вместо "Музыка"
   - Список сохраненных MP3/WAV/OGG файлов
   - Кнопки скачать и удалить

2. **Чат**:
   - Кнопка "Сохранить аудио" для MP3/WAV/OGG файлов
   - Уведомления об успешном сохранении

## Как использовать

1. В чате найдите сообщение с аудио файлом (MP3, WAV, OGG)
2. Нажмите кнопку "Сохранить аудио" под файлом
3. Файл появится в разделе "Аудио" в настройках профиля
4. Оттуда можно скачать или удалить сохраненные файлы

## Технические детали

- Сохраняются только ссылки на файлы, не дубликаты
- Один файл можно сохранить только один раз
- При удалении сообщения, сохраненная ссылка тоже удаляется (CASCADE)
- Поддерживаются форматы: MP3, WAV, OGG

## Миграция

Миграция уже применена. Если нужно применить вручную:
```bash
cd server
node apply-saved-audio-migration.js
```
