# 🚨 КРИТИЧЕСКАЯ ПРОБЛЕМА - СЕРВЕР НЕ ЗАПУЩЕН

## ❌ Ошибка
```
GET http://localhost:3003/socket.io/ net::ERR_CONNECTION_REFUSED
```

## ✅ РЕШЕНИЕ

### 1. Запустите СЕРВЕР (обязательно!)

**Вариант А: Через bat файл**
```
Двойной клик на: START_SERVER.bat
```

**Вариант Б: Вручную**
```bash
cd server
npm run dev
```

Должно появиться:
```
🚀 Сервер запущен на порту 3002
```

### 2. Запустите КЛИЕНТ

**Вариант А: Через bat файл**
```
Двойной клик на: START_CLIENT.bat
```

**Вариант Б: Вручную**
```bash
cd client
npm run dev
```

### 3. Откройте браузер
```
http://localhost:5173/chat
```

## 📋 Checklist

- [ ] Сервер запущен (порт 3002)
- [ ] Клиент запущен (порт 5173)
- [ ] Нет ошибок в консоли сервера
- [ ] Нет ошибок в консоли браузера
- [ ] Страница загружается

## 🔍 Проверка

### Сервер работает?
Откройте в браузере:
```
http://localhost:3002/health
```

Должен вернуть JSON с status: "ok"

### Клиент работает?
```
http://localhost:5173
```

Должна открыться страница логина

## ⚠️ Важно!

**НУЖНЫ ОБА ПРОЦЕССА:**
1. Сервер (backend) - порт 3002
2. Клиент (frontend) - порт 5173

Без сервера клиент не сможет:
- Подключиться к WebSocket
- Отправлять сообщения
- Загружать данные

## 🎯 Быстрый старт

### Терминал 1 (Сервер):
```bash
cd server
npm run dev
```

### Терминал 2 (Клиент):
```bash
cd client  
npm run dev
```

### Браузер:
```
http://localhost:5173/chat
```

## ✅ Все работает когда:

1. В терминале сервера:
```
✅ База данных подключена
📋 Схема базы данных инициализирована
🚀 Сервер запущен на порту 3002
```

2. В терминале клиента:
```
VITE ready in XXX ms
➜ Local: http://localhost:5173/
```

3. В консоли браузера:
```
Нет красных ошибок
```

## 🔧 Если порт занят

### Сервер (3002):
Измените в `server/.env`:
```
PORT=3002
```

### Клиент (5173):
Vite автоматически выберет другой порт

## 📝 Структура запуска

```
Проект/
├── START_SERVER.bat  ← Запустить сервер
├── START_CLIENT.bat  ← Запустить клиент
├── server/
│   └── npm run dev   → Порт 3002
└── client/
    └── npm run dev   → Порт 5173
```

## 🎉 После запуска

Все функции будут работать:
- ✅ Логин/регистрация
- ✅ Отправка сообщений
- ✅ WebSocket подключение
- ✅ Все API запросы
