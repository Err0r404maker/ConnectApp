# 🔧 Исправление счетчика непрочитанных

## Что исправлено:

### 1. Упрощена логика отметки прочитанных
- Убрана сложная логика с `lastReadMessageId`
- Теперь просто отмечаем все непрочитанные сообщения в чате
- Возвращаем количество отмеченных сообщений

### 2. Немедленное обновление UI
- Счетчик обнуляется сразу при клике (оптимистичное обновление)
- Через 500мс перезагружаем для синхронизации
- Добавлено логирование для отладки

### 3. Диагностика
Запустите для проверки БД:
```bash
node test-unread.js
```

## Как применить:

1. **Перезапустите сервер:**
```bash
cd server
npm run dev
```

2. **Очистите кэш браузера:**
- Нажмите `Ctrl+Shift+R`

3. **Проверьте консоль:**
- Должно появиться: "Отмечено как прочитанных: X"

## Если не работает:

1. Проверьте, что таблица создана:
```bash
node test-unread.js
```

2. Если таблицы нет:
```bash
node apply-schema-updates.js
```

3. Проверьте логи сервера на ошибки

## Ожидаемое поведение:

✅ При клике на чат счетчик сразу становится 0
✅ При получении нового сообщения счетчик увеличивается
✅ Счетчик синхронизируется между вкладками через WebSocket
