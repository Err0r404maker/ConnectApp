# 🔒 Безопасность проекта

## Реализованные меры безопасности

### 🛡️ Аутентификация и авторизация
- JWT токены с коротким временем жизни (15 минут)
- Refresh токены для обновления доступа
- Безопасное хеширование паролей с bcrypt (12 раундов)
- Валидация ролей пользователей

### 🚫 Защита от атак
- **CSRF защита** - токены для изменяющих операций
- **Rate limiting** - ограничение количества запросов
- **Helmet.js** - безопасные HTTP заголовки
- **CORS политика** - ограничение источников запросов
- **Валидация входных данных** - проверка всех пользовательских данных

### 🔐 Безопасность данных
- Параметризованные SQL запросы (защита от SQL injection)
- Санитизация пользовательского ввода
- Ограничения на размер загружаемых файлов
- Безопасное хранение секретных ключей в переменных окружения

### 🌐 Сетевая безопасность
- HTTPS в продакшене
- Безопасные WebSocket соединения
- Проверка источника запросов
- Защита от XSS атак

## Переменные окружения

### Сервер (.env)
```env
JWT_SECRET="32-character-random-string"
JWT_REFRESH_SECRET="another-32-character-string"
NODE_ENV=production
CORS_ORIGIN="https://your-domain.com"
```

### Клиент (.env)
```env
VITE_API_URL=https://your-domain.com/api
VITE_WS_URL=wss://your-domain.com
NODE_ENV=production
```

## Рекомендации по развертыванию

1. **Используйте HTTPS** в продакшене
2. **Настройте firewall** для ограничения доступа
3. **Регулярно обновляйте** зависимости
4. **Мониторьте логи** на предмет подозрительной активности
5. **Используйте reverse proxy** (nginx) для дополнительной защиты

## Отчет об уязвимостях

Если вы обнаружили уязвимость, пожалуйста, сообщите об этом ответственно:
- Не публикуйте детали публично
- Отправьте описание на security@your-domain.com
- Дайте время на исправление перед раскрытием