# 🚀 Деплой мессенджера на домен

## Варианты хостинга

### 1. **Vercel** (Рекомендуется для фронтенда)
- ✅ Бесплатный SSL
- ✅ Автоматический деплой из GitHub
- ✅ CDN по всему миру
- ❌ Не подходит для WebSocket сервера

### 2. **Railway** (Рекомендуется для бэкенда)
- ✅ Поддержка WebSocket
- ✅ PostgreSQL + Redis встроены
- ✅ Бесплатный план $5/месяц
- ✅ Автодеплой из GitHub

### 3. **Render**
- ✅ Полностью бесплатный
- ✅ PostgreSQL + Redis
- ✅ WebSocket поддержка
- ⚠️ Засыпает после 15 мин неактивности

### 4. **VPS (DigitalOcean, Hetzner)**
- ✅ Полный контроль
- ✅ Лучшая производительность
- ❌ Требует настройки
- 💰 От $5/месяц

---

## 🎯 Быстрый старт (Railway + Vercel)

### Шаг 1: Подготовка кода

```bash
# Создайте репозиторий на GitHub
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/messenger.git
git push -u origin main
```

### Шаг 2: Деплой бэкенда на Railway

1. Зайдите на [railway.app](https://railway.app)
2. Нажмите "Start a New Project"
3. Выберите "Deploy from GitHub repo"
4. Выберите ваш репозиторий
5. Railway автоматически определит Node.js проект

**Добавьте переменные окружения:**
```
NODE_ENV=production
PORT=3001
DATABASE_URL=postgresql://... (Railway создаст автоматически)
REDIS_URL=redis://... (Railway создаст автоматически)
JWT_SECRET=ваш-секретный-ключ-минимум-32-символа
JWT_REFRESH_SECRET=другой-секретный-ключ-минимум-32-символа
CLIENT_URL=https://ваш-домен.vercel.app
```

6. Добавьте PostgreSQL: New → Database → PostgreSQL
7. Добавьте Redis: New → Database → Redis
8. Скопируйте URL вашего бэкенда (например: `https://messenger-production.up.railway.app`)

### Шаг 3: Деплой фронтенда на Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите "Add New Project"
3. Импортируйте репозиторий из GitHub
4. Настройте проект:
   - **Framework Preset**: Vite
   - **Root Directory**: `client`
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`

**Добавьте переменные окружения:**
```
VITE_API_URL=https://ваш-бэкенд.up.railway.app
VITE_WS_URL=wss://ваш-бэкенд.up.railway.app
```

5. Нажмите "Deploy"
6. Получите ваш домен: `https://ваш-проект.vercel.app`

### Шаг 4: Настройка CORS на бэкенде

Обновите `CLIENT_URL` в Railway на ваш Vercel домен.

---

## 🌐 Подключение своего домена

### На Vercel (фронтенд):
1. Settings → Domains
2. Добавьте ваш домен (например: `messenger.ru`)
3. Настройте DNS записи у регистратора:
   ```
   Type: A
   Name: @
   Value: 76.76.21.21
   
   Type: CNAME
   Name: www
   Value: cname.vercel-dns.com
   ```

### На Railway (бэкенд):
1. Settings → Networking → Custom Domain
2. Добавьте поддомен (например: `api.messenger.ru`)
3. Настройте DNS:
   ```
   Type: CNAME
   Name: api
   Value: ваш-проект.up.railway.app
   ```

---

## 📝 Необходимые изменения в коде

### 1. Обновите `client/vite.config.ts`:
```typescript
export default defineConfig({
  server: {
    host: true,
    port: 5173
  },
  build: {
    outDir: 'dist',
    sourcemap: false
  }
})
```

### 2. Создайте `server/Procfile` для Railway:
```
web: node index.js
```

### 3. Обновите `server/package.json`:
```json
{
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
```

---

## 🔒 Безопасность

1. **Никогда не коммитьте `.env` файлы**
2. Используйте сильные JWT секреты (минимум 32 символа)
3. Включите HTTPS (автоматически на Vercel/Railway)
4. Настройте rate limiting
5. Регулярно обновляйте зависимости

---

## 📊 Мониторинг

- **Railway**: Встроенные логи и метрики
- **Vercel**: Analytics в панели управления
- **Sentry**: Для отслеживания ошибок (опционально)

---

## 💰 Стоимость

### Бесплатный вариант:
- Vercel: Бесплатно
- Railway: $5/месяц кредитов (хватит на старт)
- **Итого**: ~$0-5/месяц

### Платный вариант (для продакшена):
- Railway Pro: $20/месяц
- Vercel Pro: $20/месяц
- Домен: ~$10/год
- **Итого**: ~$40/месяц

---

## 🆘 Помощь

Если что-то не работает:
1. Проверьте логи в Railway/Vercel
2. Убедитесь, что все переменные окружения установлены
3. Проверьте CORS настройки
4. Убедитесь, что БД и Redis подключены

**Готово! Ваш мессенджер теперь доступен по домену! 🎉**
